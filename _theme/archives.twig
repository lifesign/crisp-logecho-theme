{% extends "default.twig" %}
{% block body %}


{% for item in archive %}
    <h2 class="post-title" id="archive-{{item.slug}}">{{item.name}}</h2>
<ul>
    {% for post in item.post %}
    <li><a href="{{post.url}}">{{post.title}}</a></li>
    {% endfor %}
</ul>
{% endfor %}

{% endblock %}