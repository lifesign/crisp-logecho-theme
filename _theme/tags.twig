{% extends "default.twig" %}
{% block body %}

{% for item in tag %}

<h2 class="post-title" id="tag-{{item.slug}}" >{{item.name}}</h2>
<ul>
    {% for post in item.post %}
        <li><a href="{{post.url}}">{{post.title}}</a></li>
    {% endfor %}
</ul>
{% endfor %}

{% endblock %}